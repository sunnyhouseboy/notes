可以先查看对应日志：/var/log
==安装EPEL仓库和Remi仓库==
`yum install epel-release`
`rpm -Uvh http://rpms.remirepo.net/enterprise/remi-release-7.rpm`
==安装较新版本的PHP（例如PHP 7.4）==
`yum install yum-utils`
`yum-config-manager --enable remi-php74`    注意要修改这个74
`yum update php`