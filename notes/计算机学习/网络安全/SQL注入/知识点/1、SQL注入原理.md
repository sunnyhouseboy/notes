## 一、什么是 SQL 注入
### 1、SQL注入简介
**★★漏洞原理★★：**
SQL 注入是因为后台 SQL 语句拼接了用户的输入，而且 Web 应用程序对用户输入数据的合法性没有判断和过滤，前端
传入后端的参数是攻击者可控的，攻击者可以通过构造不同的 SQL 语句来实现对数据库的任意操作。比如查询、删除，增加，修改数据等等，如果数据库的用户权限足够大，还可以对操作系统执行操作。
**sql 注入的原理**：WEB 应用程序对用户输入的数据 没有过滤或者过滤的不严谨 并且把用户输入的数据当作 SQL 语句 带入到数据中去执行。
### 2、登录请求的流程
web 应用程序三层架构：视图层 + 业务逻辑层 + 数据访问层：
正常的登录流程：
用户输入账户密码：张三/12345 ==>服务器执行sql语句==>select * from user where name = "张三" andpassword=12345；

如果这个登录存在sql注入漏洞，那么攻击者可以通过构造不同的SQL语句来实现对数据库的任意操作。
### 3、SQL 注入容易出现的地方
1. 登录框
2. 搜索框
3. 删除按钮
4. 新增按钮
简而言之，只要与数据库发生交互，都有可能存在 sql 注入漏洞。
## 二、与 MySQL 注入的相关知识
在 MySQL5.0 版本以后，默认在数据库中存放在一个叫 infomation_schema 里面这个库里面有很多表重点是这三个表columns 、tables、schemata 。
- information_schema.schemata: 该数据表存储了 mysql 数据库中的所有数据库的库名
- information_schema.tables： 该数据表存储了 mysql 数据库中的所有数据表的表名
- information_schema.columns: 该数据表存储了 mysql 数据库中的所有列的列名
## 三、Sqli-labs 实验环境搭建
往往很多新手在刚学习 SQL 注入的时候，都需要拥有一个能 SQL 注入的网站，需要有SQL 注入点 的。因此：我们一般都是在本地搭建一个能 SQL 注入测试的网站，那样我们学习 SQL 注入就容易多了。
Sqli-labs 是一个印度程序员写的，用来学习 sql 注入的一个游戏教程。
Sqli-labs 项目地址：https://github.com/Audi-1/sqli-labs
### 1、上传文件
把靶场上传到 centos7 /var/www/html 中
### 2、解压文件
`unzip sqli_lab.zip`
`mv sqli-labs-master sqli`
### 3、修改配置文件
sqli-labs 正常访问需要连接数据库，设置连接数据库配置文件 db-creds.inc 才能正常连接。
`vim sqli/sql-connections/db-creds.inc`
第五行 $dbpass 是数据库密码，设置好密码保存
![[Pasted image 20260106212358.png]]
### 4、浏览器访问靶场
浏览器打开 http://192.168.88.136/sqli
![[Pasted image 20260106232249.png]]
![[Pasted image 20260106232230.png]]
**注意**：中间要是运行不出来可以去 /var/log/httpd/error_log下查看报错情况
### 5、再次访问首页
每个例子都有对应的 Less-ID，我们直接以 Less-ID 表示对应的页面即可。
![[Pasted image 20260107155558.png]]
## 四、Sqlab 教学
### 1、Less-1 分析
![[Pasted image 20260107155753.png]]
### 2、传递参数
提示我们输入一个 ID 参数，我们修改 url 来传递 ID 参数 在链接后面加上 ?id=1即
http: 192.168.83.144/sqli/Less-1/?id=1
**参数说明**：
？表示传递参数，通常都是在页面后会有 ?id=数值，这样的方式传递参数给服务器，然后给我们返回参数对应的页面信息，我们发现我们是在/后面直接使用？传递参数，此时参数会传递给默认页面。
### 3、源码分析
查看第一关源码：`vim /var/w /html/sqli/Less-1/index.php`
![[Pasted image 20260107160435.png]]
我们只关注最核心的代码 29-30 行 ：
`$sql="SELECT * FROM users WHERE id='$id' LIMIT 0,1";`
`$result=mysql_query($sql);`
mysql_query是PHP中的一个内置函数，用于执行某个针对数据库的查询
相当于在数据库中执行了以下 sql 语句：`select * from users where id =1;`
### 4、报错信息
我们尝试在 url 后面添加一个单引号即：`http: 192.168.83.144/sqli/Less-1/?id=1'`
看一下错误信息：
![[Pasted image 20260107160755.png]]

原始查询很可能是这样的：`SELECT ... FROM ... WHERE id = ‘用户输入’ LIMIT 0,1`。当用户输入 `1'`时，查询就变成了 `... WHERE id = '1'' LIMIT 0,1`。这多出来的一个单引号导致了语法错误。
### 5、闭合方式概述
闭合方式是指开发人员在 sql 语句中的加在参数变量两边的符号,less-1 这里是单引号闭合。除了单引号 还有双引号小括号等。
“闭合方式”是连接用户输入与后端SQL语句的“桥梁规则”。黑客必须首先“猜中”或“探测出”这个规则（即闭合方式），才能构造出语法正确、能被数据库执行的恶意SQL语句。它是所有SQL注入攻击的基石，理解了它，您就理解了SQL注入为何能够发生。您之前做的添加单引号测试，正是探测这个“桥梁规则”第一步，也是最关键的一步。
闭合方式：
- **单引号闭合**，攻击载荷需要处理单引号。
- **双引号闭合**：如果代码是 `id="$id"`，攻击载荷就需要处理双引号。
- **括号+单引号闭合**：如果代码是 `id=('$id')`，攻击载荷就需要写成。
### 6、使用 order by 判断表中字段数
ORDER BY 关键词用于对记录集中的数据进行排序。
#### ①按某个字段进行排序
`select * from 表名 order by 字段名;`
#### ②按第几个字段进行排序，如果超过查询的字段数，就报错
`select * from 表名 order by 数字;`
#### ③利用 Sql 注入统计字段数量
`http: 192.168.88.136/sqli/Less-1/?id=1' order by 4 --+`
' 单引号闭合 id 字段 order by 4 判断是否存在 4 个字段 --+ 注释掉后面的 SQL 语句 ，大家都知道 不管什么编程语言只要注释掉就不会被执行。
**执行结果**：
![[Pasted image 20260107164623.png]]
## 五、几种注释符号
- --空格：数据库中可以使用，但是在浏览器地址栏、Burp Suite等HTTP工具中进行注入测试不能使用
- --+或--%20：+或 %20是URL中空格的编码形式，能确保服务器正确接收到一个空格，从而生成合法的SQL注释 -- 。
- \#在 url 中 # 号是用来指导浏览器动作的（例如锚点），对服务器端完全无用。所以，HTTP 请求中不包括 # 将#号改成url 的编码 %23 就可以了。