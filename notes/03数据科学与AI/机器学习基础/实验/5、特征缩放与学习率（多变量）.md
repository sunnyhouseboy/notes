# 特征缩放与学习率（多变量）

## 目标

在本实验中，你将：

- 利用在前一个实验中开发的多变量程序
    
- 在具有多个特征的数据集上运行梯度下降
    
- 探索 _学习率 alpha_ 对梯度下降的影响
    
- 通过使用 z-score 标准化的 _特征缩放_ 来提高梯度下降的性能
    

## 工具

你将使用在上一个实验中开发的函数，以及 matplotlib 和 NumPy。

Python

```python
import numpy as np
import matplotlib.pyplot as plt
from lab_utils_multi import load_house_data, run_gradient_descent 
from lab_utils_multi import norm_plot, plt_equal_scale, plot_cost_i_w
from lab_utils_common import dlc
np.set_printoptions(precision=2)
plt.style.use('./deeplearning.mplstyle')
```
## 符号说明

| **通用符号**                                        | **描述**                                                                                                                          | **Python (如果适用)** |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| $a$                                             | 标量（非加粗）                                                                                                                         |                   |
| $\mathbf{a}$                                    | 向量（加粗）                                                                                                                          |                   |
| $\mathbf{A}$                                    | 矩阵（加粗大写）                                                                                                                        |                   |
| $\mathbf{X}$                                    | 训练样本矩阵                                                                                                                          | `X_train`         |
| $\mathbf{y}$                                    | 训练样本目标值                                                                                                                         | `y_train`         |
| $\mathbf{x}^{(i)}$, $y^{(i)}$                   | 第 $i$ 个训练样本                                                                                                                     | `X[i]`, `y[i]`    |
| $m$                                             | 训练样本数量                                                                                                                          | `m`               |
| $n$                                             | 每个样本中的特征数量                                                                                                                      | `n`               |
| $\mathbf{w}$                                    | 参数：权重                                                                                                                           | `w`               |
| $b$                                             | 参数：偏置                                                                                                                           | `b`               |
| $f_{\mathbf{w},b}(\mathbf{x}^{(i)})$            | 模型在 $\mathbf{x}^{(i)}$ 处的评估结果（由参数 $\mathbf{w},b$ 确定）：$f_{\mathbf{w},b}(\mathbf{x}^{(i)}) = \mathbf{w} \cdot \mathbf{x}^{(i)}+b$ | `f_wb`            |
| $\frac{\partial J(\mathbf{w},b)}{\partial w_j}$ | 代价函数相对于参数 $w_j$ 的梯度或偏导数                                                                                                         | `dj_dw[j]`        |
| $\frac{\partial J(\mathbf{w},b)}{\partial b}$   | 代价函数相对于参数 $b$ 的梯度或偏导数                                                                                                           | `dj_db`           |

## 问题陈述

与之前的实验一样，你将使用房价预测的激励示例。训练数据集包含许多具有 4 个特征（面积、卧室数量、层数和房龄）的示例，如下表所示。请注意，在本实验中，“面积”特征的单位是平方英尺（sqft），而之前的实验使用的是 1000 平方英尺。该数据集比上一个实验的更大。

我们希望利用这些值建立一个线性回归模型，以便预测其他房子的价格——例如，一栋 1200 平方英尺、3 间卧室、1 层楼、房龄 40 年的房子。

## 数据集

| **面积 (sqft)** | **卧室数量** | **层数** | **房龄** | **价格 (1000 美元)** |
| ------------- | -------- | ------ | ------ | ---------------- |
| 952           | 2        | 1      | 65     | 271.5            |
| 1244          | 3        | 2      | 64     | 232              |
| 1947          | 3        | 2      | 17     | 509.8            |
| ...           | ...      | ...    | ...    | ...              |

```python
# 加载数据集
X_train, y_train = load_house_data()
X_features = ['size(sqft)','bedrooms','floors','age']
```

让我们通过绘制每个特征与价格的关系图来查看数据集及其特征。

```python
fig,ax=plt.subplots(1, 4, figsize=(12, 3), sharey=True)
for i in range(len(ax)):
    ax[i].scatter(X_train[:,i],y_train)
    ax[i].set_xlabel(X_features[i])
ax[0].set_ylabel("Price (1000's)")
plt.show()
```
![[Pasted image 20260511184527.png]]
绘制每个特征与目标值（价格）的图表，可以初步了解哪些特征对价格影响最大。从上图看，增加面积也会增加价格；卧室数量和层数似乎对价格没有强烈影响；较新的房子比旧房子价格更高。

## 多变量梯度下降

以下是你上一个实验中开发的多变量梯度下降方程：

$$\begin{align*} \text{repeat}&\text{until convergence：} ; \lbrace \newline;

& w_j := w_j - \alpha \frac{\partial J(\mathbf{w},b)}{\partial w_j} \tag{1} ; & \text{for j = 0..n-1}\newline

&b\ \ := b - \alpha \frac{\partial J(\mathbf{w},b)}{\partial b} \newline \rbrace

\end{align*}$$

其中，$n$ 是特征数量，参数 $w_j$ 和 $b$ 同时更新，并且：

$$\begin{align}
\frac{\partial J(\mathbf{w},b)}{\partial w_j} &= \frac{1}{m} \sum\limits_{i = 0}^{m-1} (f_{\mathbf{w},b}(\mathbf{x}^{(i)}) - y^{(i)})x_{j}^{(i)} \tag{2} \\
\frac{\partial J(\mathbf{w},b)}{\partial b} &= \frac{1}{m} \sum\limits_{i = 0}^{m-1} (f_{\mathbf{w},b}(\mathbf{x}^{(i)}) - y^{(i)}) \tag{3}
\end{align}$$

- $m$ 是数据集中的训练样本数量
    
- $f_{\mathbf{w},b}(\mathbf{x}^{(i)})$ 是模型的预测值，而 $y^{(i)}$ 是目标值
    

## 学习率
![[C1_W2_Lab06_learningrate.png]]
课程讨论了设置学习率 $\alpha$ 相关的问题。学习率控制参数更新的步长，它被所有参数共享。

让我们运行梯度下降，并在我们的数据集上尝试几种 $\alpha$ 的设置。

$\alpha = 9.9 \times 10^{-7}$

```python
# 将 alpha 设置为 9.9e-7
_, _, hist = run_gradient_descent(X_train, y_train, 10, alpha = 9.9e-7)
```

看起来学习率太高了。解没有收敛，代价（Cost）反而是在 _增加_ 而不是减少。让我们绘制结果：

```python
plot_cost_i_w(X_train, y_train, hist)
```
![[Pasted image 20260511184942.png]]
右侧的图显示了其中一个参数 $w_0$ 的值。在每次迭代中，它都跳过了最优值，导致代价最终增加而不是接近最小值。请注意，这并不是一个完全准确的图景，因为每次迭代有 4 个参数在修改，而不仅仅是一个。此图仅显示了 $w_0$，其他参数固定在较好的值上。在这些图中，你可能会注意到蓝色和橙色线条略有偏差。

 $\alpha = 9 \times 10^{-7}$

让我们尝试一个小一点的值，看看会发生什么。

```python
# 将 alpha 设置为 9e-7
_, _, hist = run_gradient_descent(X_train, y_train, 10, alpha = 9e-7)
```

代价在运行过程中不断减少，这表明 alpha 不算太大。

```python
plot_cost_i_w(X_train, y_train, hist)
```
![[Pasted image 20260511185023.png]]
在左侧，你可以看到代价在减小。在右侧，可以看到 $w_0$ 正在向最小值靠拢，尽管目前还在震荡。

 $\alpha = 1 \times 10^{-7}$

让我们再次尝试一个小一点的 $\alpha$ 值。

```python
# 将 alpha 设置为 1e-7
_, _, hist = run_gradient_descent(X_train, y_train, 10, alpha = 1e-7)
```

在整个运行过程中，成本（Cost）持续下降，这表明学习率 $\alpha$ 设置得并不算太大。

```python
plot_cost_i_w(X_train, y_train, hist)
```
![[Pasted image 20260511190002.png]]
在左侧，你可以看到成本（cost）正按预期逐渐降低。在右侧，你可以看到 $w_0$ 在没有越过最小值的情况下不断减小。请注意上方，`dj_w0` 在整个运行过程中始终为负值。这个解同样会收敛，尽管速度比之前的例子稍慢一些。

## 特征缩放
![[C1_W2_Lab06_featurescalingheader.png]]
课程中介绍了**重缩放数据集**（rescaling the dataset）的重要性，以确保各特征具有相似的取值范围。
### 原理
让我们再次回顾 $\alpha$ = 9e-7 时的情况。这已经非常接近我们在不发散的前提下所能设置的 $\alpha$ 最大值了。这是一个显示前几次迭代的短期运行结果：
![[C1_W2_Lab06_ShortRun.png]]
如上所示，虽然成本正在降低，但显而易见的是，由于 $w_0$ 的梯度大得多，它的进展比其他参数要快得多。

下图显示了在 $\alpha$ = 9e-7 条件下进行超长运行的结果。这需要花费数小时。
![[C1_W2_Lab06_LongRun.png]]
在上方你可以看到，成本在初始下降后减速非常缓慢。请注意 `w0` 与 `w1`、`w2`、`w3` 之间的差异，以及 `dj_dw0` 与 `dj_dw1-3` 之间的区别。`w0` 很快就达到了接近最终值的状态，且 `dj_dw0` 迅速减小到一个很小的值，表明 `w0` 已接近最终解。而其他参数的下降速度则慢得多。

这是为什么呢？有什么我们可以改进的地方吗？请看下图：
![[C1_W2_Lab06_scale.png]]
上图展示了为什么 $w$ 的更新是不均匀的：

- 所有的参数更新（$w$ 和 $b$）都共用同一个 $\alpha$。
    
- 在更新 $w$ 时，共同的误差项会乘以对应的特征（$b$ 则不用）。
    
- 特征的大小差异显著，导致某些特征的更新速度比其他特征快得多。在这种情况下，$w_0$ 乘以的是“尺寸（平方英尺）”，该数值通常 > 1000；而 $w_1$ 乘以的是“卧室数量”，通常在 2-4 之间。
    

解决方案就是**特征缩放（Feature Scaling）**。

课程中讨论了三种不同的技术：

- **特征缩放（Feature scaling）**：本质上是将每个正特征除以其最大值；或者更通用地，利用 $(x-min)/(max-min)$ 根据最小值和最大值对每个特征进行重缩放。这两种方法都能将特征归一化到 -1 到 1 的范围。前者适用于正特征，操作简单且适用于课程中的示例；后者则适用于任何特征。
    
- **均值归一化（Mean normalization）**：$x_i := \frac{x_i - \mu_i}{max - min}$
    
- **Z-score 归一化（Z-score normalization）**：我们将在下文中探讨这种方法。
### Z-score 标准化

Z-score 标准化的计算公式如下：

$$x_j^{(i)} = \frac{x_j^{(i)} - \mu_j}{\sigma_j} \tag{4}$$

其中 $j$ 是特征索引。$\mu_j$ 是特征 $j$ 的平均值，$\sigma_j$ 是其标准差。

$$\begin{align}

\mu_j &= \frac{1}{m} \sum_{i=0}^{m-1} x_j^{(i)} \tag{5} \\

\sigma^2_j &= \frac{1}{m} \sum_{i=0}^{m-1} (x_j^{(i)} - \mu_j)^2 \tag{6}

\end{align}$$

> **注意：** 在对训练集进行标准化时，必须存储用于计算的平均值和标准差。在应用模型进行预测时，必须使用这些存储的值对输入数据进行同样的缩放。

```python
def zscore_normalize_features(X):
    """
    计算 X 的 z-score 标准化。
    
    参数:
      X (ndarray (m,n))     : 输入数据，m 个样本，n 个特征
      
    返回:
      X_norm (ndarray (m,n)): 归一化后的输入
      mu (ndarray (n,))     : 每个特征的平均值
      sigma (ndarray (n,))  : 每个特征的标准差
    """
    # 找到每个特征（列）的平均值
    mu     = np.mean(X, axis=0)                 # mu 的形状为 (n,)
    # 找到每个特征（列）的标准差
    sigma  = np.std(X, axis=0)                  # sigma 的形状为 (n,)
    # 逐元素相减，并按列除以 sigma
    X_norm = (X - mu) / sigma                     

    return (X_norm, mu, sigma)

# 检查操作：可以使用工业级的机器学习库 sklearn来实现和手写的 `zscore_normalize_features` 函数完全相同的功能。
#from sklearn.preprocessing import scale
#scale(X_orig, axis=0, with_mean=True, with_std=True, copy=True)
```

让我们来看看标准化前后的数据分布。

```python
mu     = np.mean(X_train,axis=0)   
sigma  = np.std(X_train,axis=0) 
X_mean = (X_train - mu)
X_norm = (X_train - mu) / sigma      
fig,ax=plt.subplots(1, 3, figsize=(12, 3))
ax[0].scatter(X_train[:,0], X_train[:,3])
ax[0].set_xlabel(X_features[0]); ax[0].set_ylabel(X_features[3]);
ax[0].set_title("未归一化")
ax[0].axis('equal')
ax[1].scatter(X_mean[:,0], X_mean[:,3])
ax[1].set_xlabel(X_features[0]); ax[0].set_ylabel(X_features[3]);
ax[1].set_title(r"仅减去 $\mu$")
ax[1].axis('equal')
ax[2].scatter(X_norm[:,0], X_norm[:,3])
ax[2].set_xlabel(X_features[0]); ax[0].set_ylabel(X_features[3]);
ax[2].set_title(r"Z-score 归一化后")
ax[2].axis('equal')
plt.tight_layout()
plt.show()
```
![[Pasted image 20260511191131.png]]
上方的图表显示了训练集中两个参数“时间（age）”与“尺寸（size(sqft)）”之间的关系。**这些图表采用了相同的比例尺进行绘制**。

- **左图：未归一化**。“尺寸”特征的数值范围（或方差）远大于“年龄”特征。
    
- **中图**：第一步减去了每个特征的均值（平均值）。这使得特征以零为中心。虽然“年龄”特征的变化在图中很难察觉，但可以清楚地看到“尺寸”特征已经分布在零附近。
    
- **右图**：第二步除以了方差。这使得两个特征都以零为中心，并且拥有了相似的比例尺（缩放范围）。

让我们将数据归一化，并将其与原始数据进行对比。
```python
# normalize the original features  
X_norm, X_mu, X_sigma = zscore_normalize_features(X_train)  
print(f"X_mu = {X_mu}, \nX_sigma = {X_sigma}")  
print(f"Peak to Peak range by column in Raw        X:{np.ptp(X_train,axis=0)}")     
print(f"Peak to Peak range by column in Normalized X:{np.ptp(X_norm,axis=0)}")
```
![[Pasted image 20260511191246.png]]
通过归一化，每一列的**峰峰值范围**（peak to peak range）从数千倍的差距缩小到了 2-3 倍左右。

```python
fig,ax=plt.subplots(1, 4, figsize=(12, 3))
for i in range(len(ax)):
    norm_plot(ax[i], X_train[:,i], f"未归一化 {X_features[i]}")
ax[0].set_ylabel("样本数量")
fig.suptitle("归一化前的特征分布")
plt.show()

fig,ax=plt.subplots(1, 4, figsize=(12, 3))
for i in range(len(ax)):
    norm_plot(ax[i], X_norm[:,i], f"归一化后 {X_features[i]}")
ax[0].set_ylabel("样本数量")
fig.suptitle("Z-score 归一化后的特征分布")
plt.show()
```
![[Pasted image 20260511191348.png]]
![[Pasted image 20260511191321.png]]
注意，标准化后所有特征的分布都集中在 0 附近，且标准差约为 1。

现在让我们使用归一化后的数据再次运行梯度下降。注意，这次我们可以使用**大得多的 alpha 值**。

```python
w_norm, b_norm, hist = run_gradient_descent(X_norm, y_train, 1000, 1.0e-1, )
```

经过缩放后的特征能够**极其快速地**获得非常精确的结果！请注意，在这次相当短的运行结束时，每个参数的梯度都已经变得非常微小。对于使用了归一化特征的回归任务来说，0.1 的学习率是一个不错的起点。让我们绘制预测值与目标值的对比图。请注意，预测是使用归一化后的特征进行的，而图表展示时使用的是原始特征值。

```python
#predict target using normalized features  
m = X_norm.shape[0]  
yp = np.zeros(m)  
for i in range(m):  
    yp[i] = np.dot(X_norm[i], w_norm) + b_norm  
  
# plot predictions and targets versus original features    
fig,ax=plt.subplots(1,4,figsize=(12, 3),sharey=True)  
for i in range(len(ax)):  
    ax[i].scatter(X_train[:,i],y_train, label = 'target')  
    ax[i].set_xlabel(X_features[i])  
    ax[i].scatter(X_train[:,i],yp,color=dlc["dlorange"], label = 'predict')  
ax[0].set_ylabel("Price"); ax[0].legend();  
fig.suptitle("target versus prediction using z-score normalized model")  
plt.show()
```
![[Pasted image 20260511191507.png]]
结果看起来不错。有几点需要注意：

- 在拥有**多个特征**的情况下，我们无法再通过单一图表来展示结果与特征之间的关系。
    
- 生成图表时，使用的是**归一化后的特征**。任何使用从归一化训练集中学到的参数进行的预测，其输入的特征也**必须经过归一化处理**。

 **预测**：构建模型的目的在于利用它来预测不在数据集中的房屋价格。让我们预测一间拥有 1200 平方英尺、3 间卧室、1 层楼、房龄 40 年的房屋价格。请记住，你必须使用在训练数据归一化时得到的均值（mean）和标准差（standard deviation）来对这些数据进行归一化。

```python
# 首先，标准化我们的预测目标
x_house = np.array([1200, 3, 1, 40])
x_house_norm = (x_house - X_mu) / X_sigma
print(x_house_norm)
x_house_predict = np.dot(x_house_norm, w_norm) + b_norm
print(f"预测 1200 平方英尺, 3 间卧室, 1 层, 40 年房龄的房价 = ${x_house_predict*1000:0.0f}")
```
### 成本等高线（Cost Contours）
![[C1_W2_Lab06_contours.png]]
观察特征缩放的另一种方式是查看成本等高线。当特征尺度不匹配时，等高线图中成本与参数的关系图是不对称的。

在下图中，参数的比例尺已匹配。左图是特征归一化前 $w_0$（平方英尺）与 $w_1$（卧室数量）的成本等高线图。由于图形极度不对称，甚至看不见完整的等高线曲线。相比之下，当特征经过归一化后，成本等高线显得对称得多。其结果是，在梯度下降期间，各参数的更新能够以均衡的进度进行。
```python
plt_equal_scale(X_train, X_norm, y_train)
```
![[Pasted image 20260511192503.png]]