## 一、盲注的概念
在页面中不会显示数据库信息，一般情况下只会显示对与错的内容。通俗的讲就是在前端页面没有显示位，不能返回 SQL 语句执行错误的信息。盲注分为布尔盲注和时间盲注。
## 二、盲注的优缺点
**优点**：不需要显示位和报错信息。
**缺点**：速度慢，耗费大量时间。
## 三、盲注的流程
1. 判断数据库长度 和 字符
2. 判断表的长度和字符
3. 判断出字段的长度 和 字符
4. 判断出字段值的长度和字符
## 四、布尔盲注
什么时候使用布尔盲注？
- 没有返回 SQL 执行的错误信息
- 错误与正确的输入，返回的结果只有两种